.tickets/**/*.md text eol=lf
.tickets/**/*.md merge=gitticket
# Built output, committed so the Go binary can embed it and verified against
# the source by scripts/verify-dist.mjs. Nobody reads a minified diff, and a
# PR review that is handed one runs out of context before the source, which
# is what happened on PR 16. Git reports these as binary instead.
web/dist/** -diff
