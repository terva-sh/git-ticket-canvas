---
schema: 3
id: TKT-01M38QP2NVPG01307B8V69C29M
title: Choose a phone, tablet or desk layout from the window
type: task
status: review
status_reason: null
priority: normal
due_on: null
labels:
  - ui
assignees: []
milestone: null
parent: TKT-01M38QMHD99E8CKJR263X7BVDB
origin: null
dependencies: []
blocks_on: none
references: []
claim:
  actor: agent:claude/mobile-layout
  branch: worktree-agent-a4f2019162572034f
  worktree: /home/sothr/workspace/git.local.sothr.com/terva-sh/git-ticket-canvas/.claude/worktrees/agent-a4f2019162572034f
  commit: d760329b61513f256fa407da7e21003109930c5a
  session: null
  claimed_at: 2026-09-24T03:44:27Z
  expires_at: null
archive: null
created_at: 2026-09-24T03:34:56Z
updated_at: 2026-09-24T03:53:54Z
created_by:
  id: agent:claude/t3code
  name: ""
updated_by:
  id: agent:claude/mobile-layout
  name: ""
extensions: {}
---

## Description

Add `layout` to `DisplayChoices` in `web/src/platform/canvas/viewport.ts`, as a fourth setting chosen by `chooseDisplay` and overridable in the Display panel:

- `phone` when the short side of the window is under 600 CSS pixels, so a phone held landscape is still a phone
- `tablet` when the pointer is coarse and it is not a phone
- `desk` otherwise

The override is stored with the other display overrides. On its own this ticket changes nothing visible except the Display panel. Later tickets read `layout` to decide what to offer.

See `docs/mobile-design-v1.md`, "Choosing the layout".

## Acceptance criteria

- [x] chooseDisplay returns a layout, with the rules tested as a table including a landscape phone and a narrow desk window
- [x] The Display panel shows the chosen layout and lets it be overridden, stored like the other overrides
- [x] The layout is exposed to CSS (for example as a data attribute on html) so stylesheets can key on it
- [x] Nothing else changes on any layout
- [x] A stored layout override survives a reload, and an unrecognised stored value is dropped

## Implementation plan

Read before writing: `DisplayChoices` in `web/src/platform/canvas/viewport.ts` holds three fields and `chooseDisplay` fills them from `{width, height, coarse}`. `useDisplay` layers the stored overrides over it with `applyOverrides`, so a fourth field there is picked up by the hook, the reset, and the override count without changes. `recall()` in `web/src/ui/displayPreferences.ts` rebuilds the record key by key from allowlists, so a key without one is dropped on reload. `DisplayDialog` renders one row per entry in the typed `CHOICES` tuple. `App.tsx` writes `targets`, `inspector` and `toolbar` onto `document.documentElement.dataset`.

### Changes

- `viewport.ts`: `export type Layout = 'phone' | 'tablet' | 'desk'`, a `LAYOUTS` constant, a `PHONE_BELOW = 600` threshold on `Math.min(width, height)`, and `layout` in `DisplayChoices`. Order of the rule: phone first (short side under 600, whatever the pointer), then tablet (coarse pointer), then desk. Comment says why the short side: a phone turned landscape is 844x390, whose width would call it a desk.
- `viewport.test.ts`: add `layout` to every existing table row, and a second table for layout alone with a phone in both orientations, a narrow desk window, the 599/600 boundary, a tablet in both orientations, and a touch-screen monitor.
- `displayPreferences.ts`: take the allowlist from `LAYOUTS` and recall `layout`. Test that a stored layout comes back and that an unrecognised one is dropped.
- `DisplayDialog.tsx`: a fourth `Choice<'layout'>`, titled `Layout`, with labels Phone, Tablet, Desk. Placed first, because it is the choice the others sit under once later tickets read it. Dialog tests: the automatic label on a phone, a landscape phone and a desk; an override written and read back through a remount.
- `App.tsx`: `root.dataset.layout` beside the other three, and in the dependency list. No stylesheet reads it yet, so nothing else changes.
- Rebuild `web/dist`.

### Rejected

- Deriving layout from `targets` and `density` instead of storing a fourth setting. The design makes it separately overridable (a phone somebody wants to edit on sets `tablet`), and a derived value cannot be overridden without overriding what it came from.
- Width alone for phone. A landscape phone is 844 wide, over the compact threshold, and would be called a desk.
- Area or diagonal. Neither is more observable than the sides, and the short side is what decides whether a second column fits.
- Putting the layout row after the existing three. It reads as the broadest choice, so first; the order is cosmetic and any browser test that enumerates rows would show it. None exists (searched `tests/browser` for the dialog ids).

## Notes

**agent:claude/t3code** at 2026-09-24T03:40:24Z

Groomed 2026-09-24. Every seam this needs already exists, and the ticket follows the pattern TKT-01M2NHFKXWYHX69YQ7KJRW1MP0 set:

- `web/src/platform/canvas/viewport.ts`: `DisplayChoices` gains `layout: 'phone' | 'tablet' | 'desk'`, and `chooseDisplay` fills it. It is pure, so the rules get a table test beside the existing ones.
- `web/src/ui/displayPreferences.ts`: `recall()` accepts only allowlisted values per key (`DENSITIES`, `PLACEMENTS`, `TARGETS`). Add `LAYOUTS`, or a stored `layout` is dropped on every reload.
- `web/src/ui/DisplayDialog.tsx`: `CHOICES` is a typed three-tuple. It becomes four.
- `web/src/ui/App.tsx:568`: `document.documentElement.dataset` already carries `targets`, `inspector` and `toolbar`. `layout` goes beside them as `data-layout`, and that is the hook the later phone tickets style against.

The short-side rule uses `Math.min(width, height)`. Once the harness ticket lands, check it with the emulated phone in both orientations.

Unit tests are enough for this ticket: `display-preferences.test.ts`, `display-dialog.test.tsx`, and a viewport table test. It does not wait on the harness.

**agent:claude/mobile-layout** at 2026-09-24T03:53:33Z

Two things found while doing this, for whoever picks up the phone and tablet work.

A desk window dragged under 600 pixels in both directions (500×560 in the table test) is chosen as `phone`. The design says a narrow desktop window stays `desk`, which holds for any window that kept its height; one squeezed short as well has a phone's room, so this follows the rule as written rather than special-casing the mouse. A touch monitor (1920×1080, coarse) is chosen as `tablet`, as the design expects, and can be set to desk by hand.

Evidence for "nothing else changes": `grep -rn data-layout web/src` finds no stylesheet rule, the only readers of `settings.layout` are the Display panel and the `data-layout` effect in `App.tsx`, and `display-app.test.tsx` mounts a phone-shaped window with a stored `layout: 'desk'` and checks that density, targets and inspector are what the window chose. Not checked in an emulated phone in both orientations, which the grooming note asks for once the harness ticket lands.

Browser suite: the first full run on this branch failed `canvas-performance.spec.ts` "120-card board keeps pointer frames out of the inspector" with a 30s timeout and `ECONNRESET` on the fixture's `POST /api/tickets`, before any page was opened, while the machine's load average was 13 on 12 cores. The spec passed alone (14.1s) and the whole suite passed on the next run (82 passed, 6 skipped).

## Summary

`layout` is a fourth display setting, chosen from the window and overridable like the other three. Nothing reads it yet except the Display panel and the `data-layout` attribute on the document; the phone and tablet tickets key on it.

The rule is in `chooseDisplay` in `web/src/platform/canvas/viewport.ts`: `phone` when `Math.min(width, height)` is under 600 CSS pixels, whatever the pointer; `tablet` on a coarse pointer that is not a phone; `desk` otherwise. The short side rather than the width so that a phone held landscape (844×390) stays a phone; its width alone is past the compact threshold. It is the one setting that gives the same answer in both orientations, where the inspector placement is the one that must not. `Layout` and `LAYOUTS` are exported beside `ToolbarScale` and `TOOLBAR_SCALES`.

`recall()` in `web/src/ui/displayPreferences.ts` takes `layout` through its own allowlist, so a stored override comes back after a reload and a value this version does not know is dropped. `DisplayDialog` lists it first of the four rows, as `Automatic — Phone` and the like, with a hint that names the 600-pixel short side. `App.tsx` sets `document.documentElement.dataset.layout` beside `targets`, `inspector` and `toolbar`. `web/dist` is rebuilt.

Tests: a layout table in `viewport.test.ts` (phone in both orientations, a narrow desk window at 640×900, the 599/600 boundary, a tablet both ways, a touch monitor), `layout` added to the existing full table with a landscape phone row; recall of each layout and drop of an unknown one; dialog rows, labels and an override surviving a remount; and an app test that a stored `layout: 'desk'` on a phone-shaped window changes `data-layout` and nothing else. Not yet checked in an emulated phone, which waits on the harness ticket.
